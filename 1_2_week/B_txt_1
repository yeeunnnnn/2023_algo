15894

1. n을 입력받게 되면 오른쪽면, 왼쪽면, 윗면, 아랫면 부분의 길이가 각각 n이 된다. 그래서 n*4를 해야하고,
또 n은 10^9 이하 이므로 long을 사용했다.
2. 10^9에 맞는 long 변수 a 선언
a를 입력
a*4 출력

10101

1. 세 각의 합이 180인 경우와 아닌 경우가 있으므로 if문을 사용, 세 각의 합이 180이라면 2중 if문을 사용해
세 각이 같거나, 두 각이 같거나, 같은 각이 없는 경우로 나눔. 세 각의 합이 180이 아니라면 에러를 출력
2. a,b,c 선언
세 각 a,b,c를 입력
d=a+b+c, 즉 d는 세 각의 합인걸 선언
    
    d가 180 이라면 조건문 {
    세 각이 같은 경우의 조건문
    Equilateral 출력
    두 각이 같은 경우의 조건문
    Isosceles 출력
    위의 경우가 모두 아닌 경우의 조건문
    Scalene 출력
    }
    d가 180이 아니라면 조건문
    Error 출력
    

1978

1. 처음에 수의 개수를 입력받고 for문을 이용해 개수에 맞는 자연수를 입력받음.
for문으로 2부터 해당 자연수 까지 모두 나누고 조건문 하나엔 A를 j로 나누어 나머지가 0인 경우,
A는 소수가 아니므로 break문을 통해 for 반복문을 빠져나옴. 이 과정을 반복하다가 A가 j와 같아지면,
A는 소수이므로 count 변수를 증가시킴.
2. 정수형 변수 N, A, count를 선언한다.
N을 입력받는다.
for 문을 N번 반복한다.
A를 입력받는다.
for 문을 2부터 A까지 반복한다.
만약 A와 j가 같은 경우 count의 값을 1 증가시킨다.
만약 A를 j로 나눈 나머지가 0인 경우 for 문을 빠져나온다.
count의 값을 출력한다.
프로그램을 종료한다.

13241

1. if 조건문을 사용하여 b가 a보다 큰 경우, a와 b의 값을 교환.
while 반복문 안에서 두 수의 나머지를 구하기 위해 a를 b로 나눈 나머지를 r 변수에 저장.
그리고 기존 b 값을 a에 저장하고, r 값을 b에 저장. 이러한 과정을 나머지 값이 0이 될 때까지 반복.
최소공배수는 입력받은 두 수의 곱을 최대공약수로 나눈 값.
2. 정수형 변수 a, b, r를 선언한다.
a, b를 입력받는다.
a, b 값 중 작은 수를 b에 대입한다(만약 b가 더 크다면 a와 b를 바꾼다.
b가 0이 아닐 때까지 반복한다.
r에 a를 b로 나눈 나머지를 대입한다.
a에 b를 대입한다.
b에 r을 대입한다.
a와 b의 최소공배수를 출력한다((c * d) / a).

13909

1. 입력받은 정수 n의 제곱근을 구하고, 이보다 작은 자연수의 개수를 구하는 것이 목적.
for 반복문 안에서는 i를 2부터 n의 제곱근 까지 1씩 증가시키면서 반복.
연수의 개수를 저장하기 위한 변수 a 값을 1 씩 증가시킴. 반복문이 종료되면 a를 출력.
2. 정수형 변수 n과 a를 선언한다.
n을 입력받는다.
변수 a에 1을 대입한다.
for문을 i=2부터 i*i가 n+1보다 작을 때까지 반복한다.
a에 1을 더한다.
변수 a를 출력한다.